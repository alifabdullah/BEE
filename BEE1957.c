#include <stdio.h>

int main() {
    int decimalNumber;
    scanf("%d", &decimalNumber);

    // Use printf with format specifier %X to print in uppercase hexadecimal
    printf("%X\n", decimalNumber);

    return 0;
}
